{
  "name": "server",
  "version": "1.0.0",
  "description": "express+nodemon+cors+body-parser(用于处理表单数据)等等",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "junyao",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  }
}
